## flask usage

do :
pip install flask

## if it does not work then try to create a venv :

doing :
python3 -m venv .venv
and then :
source .venv/bin/activate
and then :
python -m pip install Flask

## running the flaks webapp

run :
flask --app main run